scalar Date

type News {
  id: ID!
  title: String!
  contents: [String!]!
  created_at: Date!
  last_updated_at: Date
  closed_at: Date
}

type Query {
  news(offset: Int, limit: Int): [News!]!
  valid_news(offset: Int, limit: Int): [News!]!
}
